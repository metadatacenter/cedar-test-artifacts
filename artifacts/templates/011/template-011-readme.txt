One multi field with prefLabel and altLabels
TODO: Fix UI
As of 2024-03-06, the template editor in this case generates the skos:altLabel not inside the items, but directly
into the child definitions. Should be moved down and up when multi-single mode is switched

