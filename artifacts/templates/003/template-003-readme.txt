Simple template with one text field Textfield. The template was saved, so provenance info and @id are filled.
The template is a modification of 002
Several values were added or renamed to trigger validation errors during parsing
